/**
 * isPalindrome
 *
 * When given a string, this function determines if the string is spelled the same backwards and forwards.
 *
 * @param str string
 * @returns boolean
 */
export function isPalindrome(str) {

}